spring:

  application:
    name: spring-eureka-client
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      failFast: true


logging:
  level:
    org.springframework.cloud: 'DEBUG'
